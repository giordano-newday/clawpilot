{
  "name": "clawpilot",
  "private": true,
  "description": "A personal AI agent that runs on your laptop, learns from you, and gets things done.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
